# Blackknight One Shared Framework

This directory contains reusable platform helpers for findings, scoring,
reporting, JSON export, Graph paging, validation, and utility functions.

The framework is loaded by `Import-BKSharedFramework.ps1`.

Engines should consume these helpers instead of creating duplicate local
implementations.
